# 7205V500 双目 24 帧配置

## 1. 修改驱动 i2c 从机地址

source/gmp/modules/isp/user/sensor/xm72050200/smart_sc2336x_slave3/sc2336x_cmos_slave3.c

```
xmedia_u16 g_sc2336x_slave3_addr[ISP_MAX_PIPE_NUM] = {0x60, 0x60, 0x64, 0x60};
```

->

```
xmedia_u16 g_sc2336x_slave3_addr[ISP_MAX_PIPE_NUM] = {0x60, 0x64, 0x64, 0x60};  // for sc2336p switch
```



## 2. 修改 sample sensor type

sampe/sample_base.mk

```
SENSOR0_TYPE := SMART_SC2336X_SLAVE3_MIPI_2M_24FPS_10BIT
```



## 3. 修改 ko demo board

source/gmp/modules/sysconfig/sys_config.c

```
#define SWITCH_DEMO_BOARD          1    /* SC3338/GC3003/SC200AI */
```



## 4. 修改 ISP 最大帧率限制

source/gmp/modules/isp/user/firmware/src/main/mpi_isp.c

```
f32FpsMax = 24.0;
```



## 5. 解除 VPSS/VENC 能力限制

source/gmp/modules/base/arch/xm72050200/include/xmedia_capability_inner.h 

XMEDIA_COMMON_GetCapability_XM72050500_MultiPipe()

```
		.stVpssCap =
        {
            .u32MaxOutW = XOR_ENCRYPT(4096),
            .u32MaxOutH = XOR_ENCRYPT(4096),
            .u32ChnFpsLimit1M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit2M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit3M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit4M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit5M = XOR_ENCRYPT(0),
        },
        .stVencCap =
        {
            .u32MaxW = XOR_ENCRYPT(3072),
            .u32MaxH = XOR_ENCRYPT(3072),
            .u32MaxArea = XOR_ENCRYPT(3072 * 1728),
            .u32ChnFpsLimit1M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit2M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit3M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit4M = XOR_ENCRYPT(15),
            .u32ChnFpsLimit5M = XOR_ENCRYPT(0),
        },
```

改成 

```
		.stVpssCap =
        {
            .u32MaxOutW = XOR_ENCRYPT(4096),
            .u32MaxOutH = XOR_ENCRYPT(4096),
            .u32ChnFpsLimit1M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit2M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit3M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit4M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit5M = XOR_ENCRYPT(0),
        },
        .stVencCap =
        {
            .u32MaxW = XOR_ENCRYPT(3072),
            .u32MaxH = XOR_ENCRYPT(3072),
            .u32MaxArea = XOR_ENCRYPT(3072 * 1728),
            .u32ChnFpsLimit1M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit2M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit3M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit4M = XOR_ENCRYPT(24),
            .u32ChnFpsLimit5M = XOR_ENCRYPT(0),
        },
```



## 6. sample 修改卷绕行数

有必要的话，才修改

demo/switch/sample_vio.c

```
stVpssChnBufWrap.u32BufLine = stSize.u32Height / 2 / 32 * 32;
```

=>

```
stVpssChnBufWrap.u32BufLine = stSize.u32Height;
```



## 7. Load ko

load 用 sc2336



## 8. 硬件接口

有跳帽接 sensor2